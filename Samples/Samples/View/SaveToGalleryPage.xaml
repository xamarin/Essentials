<views:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:views="clr-namespace:Samples.View"
                xmlns:viewmodels="clr-namespace:Samples.ViewModel"
                x:Class="Samples.View.SaveToGalleryPage"
                Title="Vibration">
    <views:BasePage.BindingContext>
        <viewmodels:SaveToGalleryViewModel />
    </views:BasePage.BindingContext>

    <ScrollView>
        <StackLayout Padding="12" Spacing="12">
            <Image Source="{Binding PngUrl}"/>
            <Button Text="Save image" Command="{Binding SaveCommand}"
                    CommandParameter="png"/>

            <Image Source="{Binding JpgUrl}" HeightRequest="300"/>
            <Button Text="Save image" Command="{Binding SaveCommand}"
                    CommandParameter="jpg"/>

        </StackLayout>
    </ScrollView>

</views:BasePage>
