<?xml version="1.0" encoding="utf-8" ?>
<view:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:Samples.View;assembly=Samples"
             xmlns:viewModel="clr-namespace:Samples.ViewModel;assembly=Samples"
             x:Class="Samples.View.RecorderPage"
             Title="Recording Sample">

    <view:BasePage.BindingContext>
        <viewModel:RecorderViewModel />
    </view:BasePage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Start recording"
                    VerticalOptions="CenterAndExpand" 
                    HorizontalOptions="CenterAndExpand" Command="{Binding StartRecording}"/>
            <Button Text="Stop recording"
                    VerticalOptions="CenterAndExpand" 
                    HorizontalOptions="CenterAndExpand" Command="{Binding StopRecording}"/>
        </StackLayout>
    </ContentPage.Content>
</view:BasePage>